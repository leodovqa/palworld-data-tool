{
  "name": "palworld-data-tool",
  "version": "1.0.0",
  "description": "A web-based tool to help players of the game Palworld determine the optimal set of passive skills for their Pals.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "parse": "node parse_pals.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --report-unused-disable-directives --max-warnings 0",
    "check-all": "npm run lint && prettier --check .",
    "format": "npx prettier --write . && npx eslint . --fix"
  },
  "keywords": [
    "palworld",
    "game",
    "tool"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5"
  }
}
